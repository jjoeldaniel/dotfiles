# dotfiles

My dotfiles

## Setup

1. Clone the repo to `~/.dotfiles` with:

    ```terminal
    git clone https://github.com/jjoeldaniel/dotfiles.git ~/.dotfiles
    ```

2. Run the installation with:

    ```terminal
    cd ~/.dotfiles && ./setup.sh
    ```
